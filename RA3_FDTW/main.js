/**
 * main.js
 * Lógica JS "clásica" del sitio (fuera del módulo React del formulario).
 * La validación e interactividad del formulario de adopción ahora vive
 * en /react (componentes React) — ver react/App.jsx y react/components/*.
 */

function MostrarMensaje() {
    alert("Direccionando...");
}
